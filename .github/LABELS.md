# Label guidance

Use `good first issue` only for small, documented tasks with a clear owner,
scope and verification path. Use `help wanted` when maintainers welcome outside
implementation help and can review a proposed change. Do not apply either label
to security reports, untriaged regressions, or work blocked on maintainer-only
access.
