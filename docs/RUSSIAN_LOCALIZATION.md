# Русская локализация

Русский язык является языком по умолчанию. Значение `ru` валидируется и сохраняется в settings store так же, как `en`, `fr` и `zh`; HTML-документ стартует с `lang="ru"`. Переводы используют fallback на английский только для неполных неосновных словарей.
